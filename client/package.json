{
  "name": "client",
  "private": true,
  "version": "0.0.0",
  "description": "Simple HTML/JS client for Master-Prompt-Editor",
  "scripts": {
    "dev": "python3 -m http.server 8080 --directory src",
    "build": "mkdir -p dist && cp -r src/* dist/",
    "lint": "echo 'No linting configured for simple client'",
    "preview": "python3 -m http.server 8080 --directory dist"
  }
}
